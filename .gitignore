# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# LaTeX build artefacts from cv/ — the compiled PDF is written to public/ by CI,
# so only these intermediates are ignored. Do NOT ignore cv/main.pdf's output at
# public/Mohammed-Eid-Abdelmeguid-Resume.pdf or public/images/resume/.
cv/*.aux
cv/*.fdb_latexmk
cv/*.fls
cv/*.log
cv/*.out
cv/*.synctex.gz
cv/*.toc
cv/main.pdf
